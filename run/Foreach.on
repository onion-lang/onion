import {
  java.util.ArrayList;
}

val strings: ArrayList[String] = new ArrayList[String];

strings.add("Foo");
strings.add("Bar");
strings.add("Hoge");

foreach object :String in strings {
  IO::println(object.toLowerCase);
}
